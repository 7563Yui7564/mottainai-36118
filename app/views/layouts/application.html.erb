<!DOCTYPE html>
<html>
  <head>
    <title>Mottainai36118</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to "MOTTAINAI", root_path %>
          </div>
      <%# ログインしている時は、以下を表示する %>
            <div class="nav__right">
              <%#= link_to "ログアウト", class: :nav__logout %> 
              <%#= link_to "投稿する", class: :nav__btn %>
            </div>
      <%# // ログインしている時は、上記を表示する %>
      <%# ログインしてない時は、以下を表示させる %>
            <div class="nav__right">
              <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
              <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
            </div>
      <%# // ログインしていない時は、上記を表示する %>
        </div>
      </div>
    </header>
    <%= yield %>
  </body>
</html>
